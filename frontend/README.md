# Frontend (Vite + React)

This folder contains a minimal Vite + React skeleton for Oratio.

How to run (Windows / PowerShell):

1. Install Node.js (v18+ recommended) and npm
2. From the `frontend` folder:

   npm install
   npm run dev

Vite will start the dev server (default http://localhost:5173).
